 import React from 'react';
 export default class Title extends React.Component {
     render(){
            return (
                <div class="title">Launch Tracker</div>
            );
        }
 }